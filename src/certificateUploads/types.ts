export type CertificateUploadSubmission = {
  blockId: string;
  username: string;
  certificateName: string;
  issuedDate: string;
  expiryDate: string;
  fileName: string;
  fileDataUrl: string;
  submittedAt: string;
};
